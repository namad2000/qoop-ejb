<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.qoop</groupId>
    <artifactId>qoop-ejb-bom</artifactId>
    <version>${qoop-ejb-global-core.version}</version>
    <packaging>pom</packaging>

    <properties>
        <!-- Version properties for all dependencies -->
        <qoop.version>${qoop-ejb-global-core.version}</qoop.version>

        <!-- Encoding -->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- Java Version -->
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>

        <!-- Library Versions -->
        <jackson.version>2.13.5</jackson.version>
        <spring-expression.version>5.3.28</spring-expression.version>
        <caffeine.version>2.9.3</caffeine.version>
        <jedis.version>3.9.0</jedis.version>
        <slf4j.version>1.7.36</slf4j.version>
        <logback.version>1.2.12</logback.version>
        <commons-lang3.version>3.12.0</commons-lang3.version>
        <embedded-redis.version>0.6</embedded-redis.version>
        <assertj-core.version>3.22.0</assertj-core.version>
        <qoop-ejb-cacheable.version>${qoop-ejb-global-core.version}</qoop-ejb-cacheable.version>
        <commons-pool2.version>2.11.1</commons-pool2.version>
        <json.version>20231013</json.version>
        <gson.version>2.8.9</gson.version>
        <logging-api.version>1.2-SNAPSHOT</logging-api.version>
        <qoop-ejb-global-core.version>0.0.1-SNAPSHOT</qoop-ejb-global-core.version>
        <qoop-ejb-util.version>0.0.1-SNAPSHOT</qoop-ejb-util.version>
    </properties>


    <dependencyManagement>
        <dependencies>
            <!-- Core: Jackson (Serialization) -->
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>${jackson.version}</version>
            </dependency>

            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-core</artifactId>
                <version>${jackson.version}</version>
            </dependency>

            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-annotations</artifactId>
                <version>${jackson.version}</version>
            </dependency>

            <dependency>
                <groupId>com.fasterxml.jackson.datatype</groupId>
                <artifactId>jackson-datatype-jsr310</artifactId>
                <version>${jackson.version}</version>
            </dependency>

            <dependency>
                <groupId>com.google.code.gson</groupId>
                <artifactId>gson</artifactId>
                <version>${gson.version}</version>
            </dependency>

            <dependency>
                <groupId>org.json</groupId>
                <artifactId>json</artifactId>
                <version>${json.version}</version>
            </dependency>

            <!-- Core: Spring Expression (SpEL) -->
            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-expression</artifactId>
                <version>${spring-expression.version}</version>
            </dependency>

            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-core</artifactId>
                <version>${spring-expression.version}</version>
            </dependency>

            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-beans</artifactId>
                <version>${spring-expression.version}</version>
            </dependency>

            <!-- Cache: Caffeine (L1 Local Cache) -->
            <dependency>
                <groupId>com.github.ben-manes.caffeine</groupId>
                <artifactId>caffeine</artifactId>
                <version>${caffeine.version}</version>
            </dependency>

            <!-- Cache: Jedis (L2 Distributed Cache) -->
            <dependency>
                <groupId>redis.clients</groupId>
                <artifactId>jedis</artifactId>
                <version>${jedis.version}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-pool2</artifactId>
                <version>${commons-pool2.version}</version>
            </dependency>

            <!-- Java EE 8: CDI / EJB / Annotation APIs -->
            <dependency>
                <groupId>javax.enterprise</groupId>
                <artifactId>cdi-api</artifactId>
                <version>2.0</version>
                <scope>provided</scope>
            </dependency>

            <dependency>
                <groupId>javax.ejb</groupId>
                <artifactId>javax.ejb-api</artifactId>
                <version>3.2.2</version>
                <scope>provided</scope>
            </dependency>

            <dependency>
                <groupId>javax.annotation</groupId>
                <artifactId>javax.annotation-api</artifactId>
                <version>1.3.2</version>
                <scope>provided</scope>
            </dependency>

            <dependency>
                <groupId>javax.interceptor</groupId>
                <artifactId>javax.interceptor-api</artifactId>
                <version>1.2.2</version>
                <scope>provided</scope>
            </dependency>

            <!-- Utilities -->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${commons-lang3.version}</version>
            </dependency>

            <!-- Doc OpenAPI -->
            <!-- TODO: Add springdoc-openapi for API documentation -->

            <!-- MapStruct -->
            <!-- TODO: Add mapstruct for DTO mapping -->

            <!-- Lombok -->
            <!-- TODO: Add lombok for boilerplate reduction -->

            <!-- JWT -->
            <!-- TODO: Add jjwt or nimbus-jose-jwt for JWT handling -->

            <!-- Logging -->
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>${slf4j.version}</version>
            </dependency>

            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-classic</artifactId>
                <version>${logback.version}</version>
            </dependency>

            <!-- Testing -->
            <dependency>
                <groupId>com.github.kstyrc</groupId>
                <artifactId>embedded-redis</artifactId>
                <version>${embedded-redis.version}</version>
                <scope>test</scope>
            </dependency>

            <dependency>
                <groupId>org.assertj</groupId>
                <artifactId>assertj-core</artifactId>
                <version>${assertj-core.version}</version>
                <scope>test</scope>
            </dependency>

            <!-- Internal Dependencies -->
            <dependency>
                <groupId>ir.tamin.framework.logging</groupId>
                <artifactId>logging-api</artifactId>
                <version>${logging-api.version}</version>
            </dependency>

            <dependency>
                <groupId>io.qoop</groupId>
                <artifactId>qoop-ejb-global-core</artifactId>
                <version>${qoop-ejb-global-core.version}</version>
            </dependency>

            <dependency>
                <groupId>io.qoop</groupId>
                <artifactId>qoop-ejb-util-core</artifactId>
                <version>${qoop-ejb-util.version}</version>
            </dependency>

            <dependency>
                <groupId>io.qoop</groupId>
                <artifactId>qoop-ejb-cacheable-api</artifactId>
                <version>${qoop-ejb-cacheable.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>